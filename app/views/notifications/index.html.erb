<p style="color: green"><%= notice %></p>

<div id="notifications">
  <% @active_dates.each do |(date, notifications)| %>
    <h2>
      <%= date %>
    </h2>
    <div class="notification-blue-box">
    <% notifications.each do |notification|%>
      <div class="notification">
        <h3>
            <%= notification.title %>
        </h3>
        <p>
            <%= notification.description %>
        </p>
        <%= render_if_exists notification.custom_partial, notification: notification %>
      </div>
    <% end %>
    </div>
  <% end %>

  <% @inactive_dates.each do |(date, notifications)| %>
    <h2>
      <%= date %>
    </h2>
    <div class="notification-grey-box">
      <% notifications.each do |notification|%>
        <div class="notification">
          <h3>
            <%= notification.title %>
          </h3>
          <p>
            <%= notification.description %>
          </p>
          <%= render_if_exists notification.custom_partial, notification: notification %>
        </div>
      <% end %>
˚    </div>
  <% end %>
</div>

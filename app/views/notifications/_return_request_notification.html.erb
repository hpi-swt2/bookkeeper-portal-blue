
<%= button_to t("defaults.accept"), stop_lending_path(id: notification.item.id), form_class: "notification-#{notification.id}"  %>
<%= button_to t("defaults.decline"), item_path(id: notification.item.id), method: :get %>

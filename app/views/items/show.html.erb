<p style="color: green"><%= notice %></p>
<link href="https://fonts.googleapis.com/css?family=Roboto" rel="stylesheet">
<style>
  :root {
      --Grey04: #394044;
      --Primary06: #B1063A;
      --Grey10: #C6CACC;
  }
  .Title{
      font-family: Roboto;
      font-style: normal;
      font-weight: 700;
      font-size: 20px;
      line-height: 23px;
      text-align: center;
      text-transform: capitalize;
      color: var(--Grey04);
      margin: 1em 0px 0px 0px;
  }
  .Body1{
      font-family: Roboto;
      font-style: normal;
      font-weight: 400;
      font-size: 18px;
      line-height: 21px;
      color: var(--Grey04);
  }
  #location{
      text-align: center;
  }
  .Subtitle{
      font-family: Roboto;
      font-style: normal;
      font-weight: 700;
      font-size: 18px;
      line-height: 21px;
      color: var(--Primary06);
      margin-bottom: 0px;
  }
  .Value{
      font-family: Roboto;
      font-style: normal;
      font-weight: 400;
      font-size: 18px;
      line-height: 21px;
      color: var(--Grey04);
  }
  .Caption{
      font-family: Roboto;
      font-style: normal;
      font-weight: 400;
      font-size: 12px;
      line-height: 14px;
      text-align: center;
      color: var(--Grey04);
  }
  .SecondaryButton{
      font-family: Roboto;
      font-style: normal;
      font-weight: 700;
      font-size: 18px;
      line-height: 21px;
      text-align: center;
      color: white;
      background-color: var(--Grey10);
      border-radius: 5px;
      border-color: transparent;
      padding-top: 5px;
      padding-bottom: 5px;
      padding-left: 11px;
      padding-right: 11px;
  }
  .CenteredContainer{
      margin: 0 auto;
      width: 65%;
  }
  .image{
    position:relative;
    overflow:hidden;
    padding-bottom:100%;
    max-width: 25em;
    max-height: 25em !important; 
  }
  .image img{
    position:absolute;
    width: 100%;
    height: 100%;
    object-fit: cover;
    max-height: 25em !important;
  }

  @media screen and (min-width: 768px) {
    .image{
      position:relative;
      overflow:hidden;
      padding-bottom:20em;
      width: 400px;
      height: 400px !important; 
    }
    .image img{
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translateX(-50%) translateY(-50%);
      border-radius: 5px;
    }
    .CenteredContainer{
      margin: 0 auto;
      width: 30%;
    }
  }
  
</style>
<div>
  <div class="row">
  <div class="CenteredContainer">

      <div class="image container-fluid rounded">
      <%= image_tag @item.image, class: "img img-responsive img-fluid rounded mx-auto d-block"%>
      </div>

    <p class="Title"><%=@item.name %></p>

    <p class="Body1" id="location"><%= @item.location %></p>
      <br>
 
      <p class="Subtitle">Autor</p>
        <p class="Body1"><%= "Book specific; doesn't exist yes ^_-"%></p>
      <p class="Subtitle">Kategorie</p>
        <p class="Body1"><%= @item.category %></p>
      <p class="Subtitle">Besitzer</p>
        <p class="VaBody1lue"><%= User.find(@item.owner).email %></p>
      <p class="Subtitle">Ausleihe bis</p>
        <p class="Body1"><%= (@item.rental_start + @item.rental_duration_sec).strftime('%d.%m.%Y') %></p>

          <div class="d-flex justify-content-center" id="buttonDiv">
          <!-- change to borrow item path when implemented -->
          <%= button_to "Auf die Warteliste", new_item_path(@item), method: :post, class: "SecondaryButton" %>
          <!--%= button_to "Ausleihen", new_item_path(@item), method: :post, class: "Primary+Button" %-->
          </div>

          <br><br><br>
    </div>
  </div>

</div>
